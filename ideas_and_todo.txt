// LATER IMPLEMENTATION:
// Filter on categories, completion status and owned



// INNER JOIN queries for contents:

SELECT content.content_id, movie_id, title, author, release_year, language, no_pages, isbn, completion_status, owned, user_rating   
FROM user_content     
INNER JOIN content
ON user_content.content_id = content.content_id
INNER JOIN book
ON content.type_specific_id = book.book_id
WHERE content.content_type = 'book';

SELECT content.content_id, movie_id, title, director, release_year, language, length, imdb_id, completion_status, owned, user_rating   
FROM user_content     
INNER JOIN content
ON user_content.content_id = content.content_id
INNER JOIN movie
ON content.type_specific_id = movie.movie_id
WHERE content.content_type = 'movie';

SELECT content.content_id, series_id, title, release_year, language, no_seasons, imdb_id, completion_status, owned, user_rating   
FROM user_content     
INNER JOIN content
ON user_content.content_id = content.content_id
INNER JOIN series
ON content.type_specific_id = series.series_id
WHERE content.content_type = 'series';

SELECT content.content_id, title, completion_status, owned, user_rating
FROM user_content
INNER JOIN content
ON user_content.content_id = content.content_id
WHERE content.content_type = NULL;